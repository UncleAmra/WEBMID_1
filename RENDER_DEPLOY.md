# Render 部署指南

## 步驟 1：準備 GitHub 倉庫

```bash
# 1. 初始化 Git（如果還沒有）
cd groupbuy_system
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit - Group Buying Platform"

# 4. 創建 GitHub 倉庫
# 到 https://github.com/new 創建新倉庫（例如：groupbuy-platform）

# 5. 連接並推送
git remote add origin https://github.com/你的用戶名/groupbuy-platform.git
git branch -M main
git push -u origin main
```

## 步驟 2：在 Render 創建服務

### A. 創建 PostgreSQL 資料庫

1. 登入 [Render](https://render.com)
2. 點擊 **"New +"** → **"PostgreSQL"**
3. 填寫資訊：
   - **Name**: `groupbuy-db`
   - **Database**: `groupbuy_system`
   - **User**: `groupbuy_user`
   - **Region**: 選擇離你最近的（Singapore 或 Oregon）
   - **Plan**: Free
4. 點擊 **"Create Database"**
5. **重要**：複製 **Internal Database URL**（等下會用到）

### B. 創建 Web Service

1. 點擊 **"New +"** → **"Web Service"**
2. 連接你的 GitHub 倉庫
3. 填寫設定：
   - **Name**: `groupbuy-platform`
   - **Region**: 與資料庫相同
   - **Branch**: `main`
   - **Root Directory**: 留空（如果專案在根目錄）
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`
   - **Plan**: Free

4. 添加環境變數（Environment Variables）：
   - 點擊 **"Advanced"**
   - 添加：
     ```
     DATABASE_URL = <你剛才複製的 Internal Database URL>
     SECRET_KEY = <隨機生成一個密鑰，例如：your-secret-key-here-change-in-production>
     ```

5. 點擊 **"Create Web Service"**

### C. 初始化資料庫

部署完成後（大約 2-3 分鐘）：

1. 訪問：`https://你的應用名稱.onrender.com/init-db`
2. 看到成功訊息後，訪問：`https://你的應用名稱.onrender.com`

## 步驟 3：分享給教授

直接給教授網址：
```
https://你的應用名稱.onrender.com
```

預設帳號：
- 用戶名：`admin`
- 密碼：`admin123`

## 注意事項

1. **免費版限制**：
   - 閒置 15 分鐘後會休眠
   - 第一次訪問需要等待 30 秒喚醒
   - 每月 750 小時免費使用時間

2. **喚醒技巧**：
   - 在展示前 1 分鐘先打開網址
   - 或使用 [UptimeRobot](https://uptimerobot.com) 定期 ping（保持喚醒）

3. **資料保存**：
   - 免費 PostgreSQL 保存 90 天
   - 之後資料會被刪除（展示用足夠了）

## 故障排除

### 問題 1：應用無法啟動
- 檢查 Render 的 Logs
- 確認 `requirements.txt` 包含所有依賴
- 確認 `gunicorn` 已添加到 requirements.txt

### 問題 2：資料庫連接失敗
- 確認 DATABASE_URL 正確複製
- 確認資料庫和 Web Service 在同一 Region

### 問題 3：靜態文件無法載入
- 確認 `static` 資料夾已提交到 Git
- 檢查 `base.html` 中的靜態文件路徑

## 優點

✅ 教授只需打開網址  
✅ 不需要安裝任何東西  
✅ 自動 HTTPS  
✅ 永久在線（免費版有限制）  
✅ 支援團隊協作  

## 給教授的使用說明

```
親愛的教授：

這是我們組的團購平台專案。您可以直接訪問以下網址查看：

🌐 網址：https://你的應用名稱.onrender.com

📝 測試帳號：
   用戶名：admin
   密碼：admin123

⚠️ 注意：
   首次訪問可能需要等待 30 秒（免費方案限制）
   請稍候片刻即可正常使用

功能展示：
1. 註冊/登入系統
2. 瀏覽團購列表
3. 查看團購詳情
4. 開新團購
5. 加入團購
6. 查看訂單
7. 後台管理

感謝您的指導！

Group 8
```
