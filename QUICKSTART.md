# 團購系統 - 快速開始指南

## 📦 專案完成內容

### ✅ Backend (Flask)
- **完整的用戶系統** - 註冊、登入、權限管理
- **四個核心資料表** - Users, Products, GroupBuying, Orders
- **完整的路由實作** - 8 個主要路由 + 管理路由
- **業務邏輯** - 團購進度計算、自動狀態更新、權限檢查

### ✅ Frontend (SB Admin 2)
- **8 個完整頁面** - 登入、註冊、瀏覽、詳情、開團、訂單、後台
- **響應式設計** - 支援手機、平板、電腦
- **即時更新** - 進度條、狀態顯示
- **美觀介面** - 使用 Bootstrap 4 + SB Admin 2

### ✅ 資料庫設計
完全符合投影片需求：
- Users → Orders → GroupBuying → Products

## 🚀 本地測試步驟

### 1. 解壓縮專案
```bash
tar -xzf groupbuy_system.tar.gz
cd groupbuy_system
```

### 2. 安裝依賴
```bash
pip3 install -r requirements.txt --break-system-packages
```

### 3. 設置資料庫（使用你現有的）
編輯 `app.py` 第 11 行：
```python
DATABASE_URL = 'postgresql://postgres:thisisme10@localhost:5432/groupbuy_system'
```

在 PostgreSQL 創建資料庫：
```bash
psql -U postgres
CREATE DATABASE groupbuy_system;
\q
```

### 4. 啟動應用
```bash
python3 app.py
```

### 5. 初始化資料庫
開啟瀏覽器訪問：
```
http://localhost:5000/init-db
```

這會自動創建：
- 所有資料表
- 管理員帳號 (admin / admin123)
- 3 個測試商品

### 6. 開始使用
```
http://localhost:5000
```

## 📱 功能測試清單

### 基本功能
- [ ] 註冊新帳號
- [ ] 登入系統
- [ ] 瀏覽團購列表
- [ ] 查看團購詳情

### 團長功能
- [ ] 創建新團購
- [ ] 查看團購進度
- [ ] 查看參與成員
- [ ] 關閉/刪除團購

### 用戶功能
- [ ] 加入團購
- [ ] 查看訂單歷史
- [ ] 計算價格

### 管理功能
- [ ] 查看統計數據
- [ ] 管理所有團購
- [ ] 搜尋過濾

## 🌐 部署到 Render

### 準備工作
1. 確保專案在 GitHub 上
```bash
git init
git add .
git commit -m "Team project"
git branch -M main
git remote add origin <你的 GitHub URL>
git push -u origin main
```

### Render 設定
1. **創建 PostgreSQL**
   - New → PostgreSQL
   - 複製 Internal Database URL

2. **創建 Web Service**
   - New → Web Service
   - 連接 GitHub repo
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app`
   - Add Environment Variable:
     - `DATABASE_URL` = <PostgreSQL URL>
     - `SECRET_KEY` = <random-string>

3. **初始化**
   - 訪問：`https://你的網址.onrender.com/init-db`

## 🎯 重要路由

| 路由 | 功能 | 方法 |
|------|------|------|
| `/` | 首頁 | GET |
| `/register` | 註冊 | GET, POST |
| `/login` | 登入 | GET, POST |
| `/browse` | 瀏覽團購 | GET |
| `/group/<id>` | 團購詳情 | GET |
| `/create_group` | 開團 | GET, POST |
| `/join_group/<id>` | 跟團 | POST |
| `/my-orders` | 我的訂單 | GET |
| `/admin` | 後台管理 | GET |

## 💡 預設帳號

**管理員**
- 用戶名: `admin`
- 密碼: `admin123`

**測試商品**
- iPhone 15 Pro - $999.99
- MacBook Air M3 - $1299.99
- AirPods Pro - $249.99

## 🔧 常見問題

### Port 5000 被占用
```bash
# 查看占用的 process
lsof -ti:5000

# 結束 process
kill -9 $(lsof -ti:5000)
```

### 資料庫連接失敗
確認：
1. PostgreSQL 正在運行
2. 資料庫已創建
3. 用戶名/密碼正確
4. Port 5432 可用

### 靜態資源無法載入
確認：
1. `static` 資料夾完整
2. 包含 `css`, `js`, `vendor`, `img` 目錄

## 📝 專案結構

```
groupbuy_system/
├── app.py                 # 主應用程式
├── models.py              # 資料庫模型
├── requirements.txt       # Python 依賴
├── README.md              # 詳細說明
├── .gitignore            # Git 忽略檔案
├── static/               # 靜態資源
│   ├── css/              # 樣式表
│   ├── js/               # JavaScript
│   ├── vendor/           # 第三方庫
│   └── img/              # 圖片
└── templates/            # HTML 模板
    ├── base.html         # 基礎模板
    ├── login.html        # 登入頁
    ├── register.html     # 註冊頁
    ├── browse.html       # 瀏覽團購
    ├── group_detail.html # 團購詳情
    ├── create_group.html # 開團頁面
    ├── my_orders.html    # 訂單歷史
    └── admin.html        # 後台管理
```

## 🎨 設計特色

1. **SB Admin 2 專業模板** - 企業級外觀
2. **響應式設計** - 支援所有裝置
3. **進度視覺化** - 直觀的進度條
4. **狀態標籤** - 清楚的顏色區分
5. **操作提示** - 友善的用戶體驗

## 👥 適用場景

- 辦公室團購
- 學校班級團購
- 社區團購
- 朋友團購
- 任何需要集體購買的場合

## 📊 符合投影片需求

✅ Frontend - 簡單操作、清晰流程
✅ Backend - 完整路由、業務邏輯
✅ Database - 四個主要資料表
✅ System Architecture - 三層架構
✅ 與 Booking System 的差異 - 團購特性
✅ Admin Management - 完整後台

祝使用愉快！🎉
